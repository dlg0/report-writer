{
  "name": "report-writer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo 'Linting not yet configured' && exit 0",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integ": "vitest run tests/integration",
    "test:e2e": "playwright test",
    "test:ci": "npm run lint && npm test && npm run test:integ && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@testing-library/jest-dom": "^6.1.5",
    "@testing-library/react": "^14.1.2",
    "@vitest/ui": "^1.0.4",
    "convex-test": "^0.0.40",
    "start-server-and-test": "^2.0.3",
    "vitest": "^1.0.4"
  },
  "dependencies": {
    "convex": "^1.29.3"
  }
}
